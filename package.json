{
  "name": "appstream-revit-deployment",
  "version": "1.0.0",
  "description": "Deploy AWS AppStream instances with Autodesk Revit, persistent S3 storage, and secure user restrictions",
  "main": "lib/appstream-stack.js",
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "deploy:custom": "./scripts/deploy.sh"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.178.2",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^4.9.5"
  }
}
